channel_targets:
  - gnuradio-oot-forge main

# Pin to Qwt version that is built with Qt5, because gnuradio uses Qt5
qwt:
  - 6.2

# conda-forge boost migration is slow, all relevant packages are 1.90 now
libboost_devel:
  - 1.90
libboost_headers:
  - 1.90
libboost_python_devel:
  - 1.90
